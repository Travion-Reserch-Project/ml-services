# DagsHub Configuration for ML Services
# 1) Copy this file to .env
# 2) Fill in your actual DagsHub username and personal access token

# DagsHub / MLflow Authentication
MLFLOW_TRACKING_USERNAME=your-dagshub-username
MLFLOW_TRACKING_PASSWORD=your-dagshub-token

# Alternatively, you can provide a single DAGSHUB_TOKEN (used if MLFLOW_* not set)
DAGSHUB_TOKEN=your-dagshub-token

# Repository Info (used to build MLflow URI if not provided explicitly)
DAGSHUB_REPO_OWNER=iamsahan
DAGSHUB_REPO_NAME=ml-services

# Optional: Explicit MLflow Tracking URI
# If set, this takes precedence over owner/name above
# Example for this project:
# MLFLOW_TRACKING_URI=https://dagshub.com/iamsahan/ml-services.mlflow
