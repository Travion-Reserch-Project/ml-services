# Production Environment Variables Template
# NEVER commit actual secrets! This is a TEMPLATE ONLY.
# 
# Instructions:
# 1. Copy this file: cp .env.production.example .env.production
# 2. Fill in actual values from secure secret management
# 3. .env.production is in .gitignore and won't be committed
# 4. Load before starting: source .env.production

# ============================================================
# MongoDB Atlas Configuration
# ============================================================
DATA_SOURCE=mongodb
MONGODB_DB_NAME=transport_service
# Get this from MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/?appName=AppName
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?appName=YOUR_APP

# ============================================================
# MLflow / DagsHub Configuration
# ============================================================
DAGSHUB_REPO_OWNER=iamsahan
DAGSHUB_REPO_NAME=ml-services
# Get from DagsHub account settings > Personal Access Tokens
DAGSHUB_TOKEN=your-dagshub-token-here

# ============================================================
# API Configuration
# ============================================================
API_HOST=0.0.0.0
API_PORT=8001

# ============================================================
# Deployment Notes
# ============================================================
# For Docker Compose:
#   env_file:
#     - .env.production
#
# For Docker CLI:
#   docker run --env-file .env.production -p 8001:8001 image-name
#
# For Kubernetes (create secret from file):
#   kubectl create secret generic transport-secrets \
#     --from-env-file=.env.production
#
# For systemd service:
#   EnvironmentFile=/path/to/.env.production
