# Travion AI Engine Dependencies
# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LangGraph & LangChain
langgraph>=0.0.40
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.0

# LLM Integration
langchain-google-genai>=1.0.0  # Gemini (Primary - Free tier)
langchain-openai>=0.0.5        # OpenAI (Fallback)
langchain-ollama>=0.0.1        # Ollama (Local fallback)

# Vector Database
chromadb>=0.4.0

# OpenAI (for embeddings - required for vector search)
openai>=1.0.0

# Web Search (Tavily)
tavily-python>=0.3.0

# Astronomy (Golden Hour calculations)
astral>=3.2
pysolar>=0.10  # High-precision NREL SPA algorithm (optional fallback)
pytz>=2023.3   # Timezone handling

# ML/Data Processing
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# HTTP Client
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
